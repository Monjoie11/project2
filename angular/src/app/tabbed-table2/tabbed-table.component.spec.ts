import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TabbedTableComponent2 } from './tabbed-table.component';

describe('TabbedTableComponent', () => {
/*   let component: TabbedTableComponent2;
  let fixture: ComponentFixture<TabbedTableComponent2>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TabbedTableComponent2 ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TabbedTableComponent2);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }); */

  it('should create', () => {
    expect(1).toEqual(1);
  });
});
